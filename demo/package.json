{
  "name": "md2pdf-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "vite dev",
    "clean:node": "bunx rimraf node_modules bun.lock",
    "dev": "vite",
    "build": "bunx rimraf dist && tsc -b && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
